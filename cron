# cron to periodically update all my dotfiles
# min hour dom month "day of week sun=0"
0 0 * * * cd ~/dotfiles/ && git pull && ./install.sh
0 0 * * * if [[ -d ~/tmp  ]]; then find ~/tmp -atime +21 -exec rm -rf {} \; ; fi;
